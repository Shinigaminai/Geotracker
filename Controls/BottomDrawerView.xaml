<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="Geotracker.Controls.BottomDrawerView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="This">
    <!--
    <Grid x:Name="DrawerContainer" VerticalOptions="End">

        <Button
            x:Name="FloatingBtn"
            Margin="20"
            BackgroundColor="{StaticResource PrimaryDark}"
            Clicked="_onButtonClicked"
            HeightRequest="40"
            HorizontalOptions="End"
            Text="Add trail"
            VerticalOptions="Start" />

        <Border
            Margin="0,80,0,0"
            BackgroundColor="{StaticResource Secondary}"
            HorizontalOptions="Fill"
            StrokeShape="RoundRectangle 30,30,0,0"
            VerticalOptions="Fill">

            <Grid RowDefinitions="Auto, *">

                <Border
                    Grid.Row="0"
                    Margin="0,10,0,10"
                    BackgroundColor="{StaticResource SecondaryDarkText}"
                    HeightRequest="10"
                    HorizontalOptions="Center"
                    StrokeShape="RoundRectangle 15"
                    VerticalOptions="Start"
                    WidthRequest="90" />

                <ContentPresenter Content="{Binding Content, Source={x:Reference This}}" />

                <Grid.GestureRecognizers>
                    <PanGestureRecognizer PanUpdated="OnDrawerPanUpdated" />
                </Grid.GestureRecognizers>
            </Grid>
        </Border>
    </Grid>-->
</ContentView>
